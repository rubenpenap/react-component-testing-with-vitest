# User events

- How to interact with the elements in the UI.
- Remove the previous `.toBeVisible()` assertions from the input and the button because interacting with those elements _implies_ they are visible and accessible (implicit assertions WTF!).

## Materials

- [Implicit assertions](https://www.epicweb.dev/implicit-assertions)
